---
deployment:
  tasks:
    - export DEPLOYPATH=/home/mygrocercom/public_html/
    - /bin/cp -R index.html $DEPLOYPATH
    - /bin/cp -R assets $DEPLOYPATH
    - /bin/cp -R images $DEPLOYPATH
    - '/bin/cp -R "brand assets" $DEPLOYPATH'
